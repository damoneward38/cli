# Exclude sourcemaps from npm package
# (They are uploaded to PostHog separately via GitHub Actions)
*.map
dist/**/*.map

# Exclude standalone binaries and templates tarball (used only for Homebrew / direct downloads)
dist/binaries/
dist/assets.tar.gz
